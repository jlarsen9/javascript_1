//make a function that takes the string and returns the first character of the string

function getFirstCharacter(sentence) {
  var index = 0;
  var firstCharacter = sentence.charAt(index);
  sentence.charAt(index);
  return firstCharacter;
}

var a = getFirstCharacter("hello");
console.log(a);
